PROJECT(nasm-linux-x86-hello-world ASM)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET (CMAKE_ASM_NASM_OBJECT_FORMAT "elf")
ENABLE_LANGUAGE(ASM_NASM)

FILE(GLOB ASM_SOURCES "*.asm")

ADD_EXECUTABLE(hello-world ${ASM_SOURCES})
